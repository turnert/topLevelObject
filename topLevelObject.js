function TopLevelObject(config) {
    if(!(this instanceof TopLevelObject) {
        return new TopLevelObject(config);
    }

    var topLevelObject = {};

    

    return topLevelObj;
}

module.exports = topLevelObject;
